name: Publish PyCharm plugin

on:
  push:
    branches: [main]
    paths:
      - "crates/**"
      - "src/**"
      - "index.html"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"
      - "vite.config.ts"
      - "plugins/pycharm/**"
      - "scripts/build_pycharm_plugin.py"
      - "scripts/stage_analyzers.py"
      - "scripts/check_cli_bundles.py"
      - "scripts/check_ide_bundles.py"
      - "scripts/stage_jscpd_wheel.py"
      - "third_party/jscpd/LICENSE"
      - "shared/**"
      - ".github/workflows/publish-pycharm-marketplace.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pycharm-marketplace-${{ github.ref }}
  cancel-in-progress: false

jobs:
  analyzer:
    name: Build analyzer (${{ matrix.platform }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux-x64
            runner: ubuntu-22.04
            executable: analyze
          - platform: windows-x64
            runner: windows-latest
            executable: analyze.exe
          - platform: macos-x64
            runner: macos-15-intel
            executable: analyze
          - platform: macos-arm64
            runner: macos-15
            executable: analyze
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo build --locked --release --bin analyze
      - name: Stage analyzer
        shell: bash
        run: |
          mkdir -p "analyzer/${{ matrix.platform }}"
          cp "target/release/${{ matrix.executable }}" "analyzer/${{ matrix.platform }}/"
      - name: Stage jscpd v5 and its license
        run: python scripts/stage_jscpd_wheel.py "analyzer/${{ matrix.platform }}"
      - name: Smoke-test packaged diagnostics
        run: python scripts/check_cli_bundles.py --binary "analyzer/${{ matrix.platform }}/${{ matrix.executable }}"
      - uses: actions/upload-artifact@v4
        with:
          name: analyzer-${{ matrix.platform }}
          path: analyzer/${{ matrix.platform }}/
          if-no-files-found: error

  publish:
    name: Upload to JetBrains Marketplace
    needs: analyzer
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - uses: actions/download-artifact@v4
        with:
          pattern: analyzer-*
          path: analyzer-artifacts
      - name: Bundle analyzers
        run: python scripts/stage_analyzers.py analyzer-artifacts
      - name: Verify standalone CLI diagnostics bundles
        run: python scripts/check_cli_bundles.py
      - name: Download PyCharm build SDK
        run: |
          curl --fail --location --retry 3 --output "$RUNNER_TEMP/pycharm.tar.gz" https://download.jetbrains.com/python/pycharm-2026.2.3.tar.gz
          mkdir -p "$RUNNER_TEMP/pycharm"
          tar -xzf "$RUNNER_TEMP/pycharm.tar.gz" -C "$RUNNER_TEMP/pycharm" --strip-components=1
      - name: Set plugin version
        run: |
          python3 -c 'import datetime, os; now = datetime.datetime.now(datetime.timezone.utc); patch = now.day * 10000000 + int(os.environ["GITHUB_RUN_NUMBER"]) * 1000 + int(os.environ["GITHUB_RUN_ATTEMPT"]); print(f"MODULELOOM_VERSION={now.year}.{now.month}.{patch}")' >> "$GITHUB_ENV"
      - name: Build plugin
        env:
          PYCHARM_HOME: ${{ runner.temp }}/pycharm
        run: python scripts/build_pycharm_plugin.py --build-only
      - name: Verify bundled diagnostics files
        run: python scripts/check_ide_bundles.py --jar target/pycharm-plugin/moduleloom.jar
      - uses: actions/upload-artifact@v4
        with:
          name: ModuleLoom-PyCharm-${{ github.run_number }}.${{ github.run_attempt }}
          path: target/pycharm-plugin/moduleloom.jar
          if-no-files-found: error
      - name: Upload plugin update
        env:
          JETBRAINS_MARKETPLACE_TOKEN: ${{ secrets.JETBRAINS_MARKETPLACE_TOKEN }}
        run: |
          if [ -z "$JETBRAINS_MARKETPLACE_TOKEN" ]; then
            echo "JETBRAINS_MARKETPLACE_TOKEN is not configured. Add it after creating the plugin listing; Marketplace upload skipped."
            exit 1
          fi
          curl --fail-with-body --show-error --silent \
            --header "Authorization: Bearer $JETBRAINS_MARKETPLACE_TOKEN" \
            --form "xmlId=com.moduleloom" \
            --form "file=@target/pycharm-plugin/moduleloom.jar" \
            https://plugins.jetbrains.com/api/updates/upload
