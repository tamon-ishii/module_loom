## 1. Project Setup and Rust Engine Scaffolding

- [x] 1.1 Tauri プロジェクトおよび Rust クレート構成を初期化し、`cargo check` およびフロントエンドビルドが通ることを検証する
- [x] 1.2 Python AST パース用の依存ライブラリ（Tree-sitter / AST パーサー）を導入し、サンプル Python ファイルの構文木生成テストが通ることを検証する

## 2. Rust Static Analysis Engine

- [x] 2.1 Python ファイル群からインポート文（絶対/相対）を抽出するモジュール依存関係抽出器を実装し、単体テストで依存グラフ生成を検証する
- [x] 2.2 強連結成分分解等を用いた循環インポート（Circular Imports）検出アルゴリズムを実装し、直接・間接サイクルの検知テストが通ることを検証する
- [x] 2.3 モジュール行数 (LOC)、関数/クラス数、結合度のメトリクス計算機能を実装し、閾値超過時の肥大化モジュール検出テストを検証する
- [x] 2.4 型アノテーションおよび型チェック診断（ty / mypy / pyright 等）の統合処理を実装し、診断メッセージがモジュールノードに紐付くことをテストで検証する

## 3. Tauri Desktop GUI & Graph Visualization

- [x] 3.1 Web フロントエンドに Canvas/WebGL ベースのグラフレンダラーを導入し、モジュールノードと依存エッジが滑らかに描画されることをプレビューで検証する
- [x] 3.2 循環インポートの警告ハイライト表示（強調エッジ・カラー）および肥大化モジュールのサイズ/警告表示を実装し、UI 上のスタイル変化を検証する
- [x] 3.3 モジュールノード選択時の詳細インスペクターパネル（LOC、インポート一覧、循環参照状態、型診断一覧）を実装し、選択時の表示を検証する
- [x] 3.4 モジュール検索およびフィルタリング機能（外部ライブラリ非表示、循環参照ノード抽出、肥大化モジュール抽出）を実装し、絞り込み動作を検証する

## 4. External Editor Integration

- [x] 4.1 PyCharm 連携機能（JetBrains URL スキーム / `pycharm` CLI コマンド呼び出し）を実装し、対象ファイルと指定行へのジャンプ呼び出しを検証する
- [x] 4.2 VS Code 連携機能（`vscode://` URL スキーム / `code` CLI 呼び出し）を実装し、対象ファイルと指定行へのジャンプ呼び出しを検証する
- [x] 4.3 優先エディタの設定画面（検出されたエディタの選択・保存）を実装し、設定保存とジャンプ先切り替えを検証する

## 5. End-to-End Integration and Verification

- [x] 5.1 サンプル Python プロジェクトに対する一連の解析・グラフ描画・循環参照警告・エディタジャンプの総合動作確認を実施・検証する
